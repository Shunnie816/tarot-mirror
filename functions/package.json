{
  "name": "@tarot-mirror/functions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "_engines": "firebase-tools はランタイムの版をここから読む。範囲指定（>=22）は受け付けない。",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "esbuild": "^0.28.1",
    "typescript": "^5.7.2",
    "zod": "^4.4.3"
  },
  "_dependencies": "ワークスペースのパッケージはここに書かない。npm が workspace:* を解釈できず、Cloud Functions のビルド（npm install）がマニフェストを読んだ時点で落ちる。esbuild が束ねてしまうので、書く必要もない。解決は build.mjs / tsconfig.json / vitest.workspace.ts の3か所の別名で行う。"
}
